export enum Sexo {
    Masculino = 1,
    Feminino = 2
}

export enum TipoPartida {
    SIMPLES_MASCULINA = 0,
    SIMPLES_FEMININA = 1,
    DUPLAS_MASCULINA = 2,
    DUPLAS_FEMININA = 3,
    DUPLAS_MISTA = 4
}